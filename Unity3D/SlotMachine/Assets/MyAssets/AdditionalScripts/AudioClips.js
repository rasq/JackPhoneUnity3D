#pragma strict

public var debug 				: boolean = false;

public var audioClips 			: AudioClip[];
public var audioGO 				: AudioSource;


//----------------------------------------------------------playClip----------------------------------------------------------------------------
function playClip(msg : int){
	     GetComponent.<AudioSource>().clip = audioClips[msg];
	     GetComponent.<AudioSource>().Play();
     
     yield WaitForSeconds(GetComponent.<AudioSource>().clip.length);
}
//----------------------------------------------------------playClip----------------------------------------------------------------------------
//----------------------------------------------------------loopNTimesClip----------------------------------------------------------------------
function loopNTimesClip(msg : int, msg1 : int){

}
//----------------------------------------------------------loopNTimesClip----------------------------------------------------------------------
//----------------------------------------------------------fadeOut-----------------------------------------------------------------------------
function fadeOut(){
	var t 						: float = 0.0;
	
	while (t > 0.1) {
         t -= Time.deltaTime;
         GetComponent.<AudioSource>().volume = t;
         yield;
	}
}
//----------------------------------------------------------fadeOut-----------------------------------------------------------------------------
//----------------------------------------------------------fadeIn------------------------------------------------------------------------------
function fadeIn(){
	var t 						: float = 0.0;
	
	while (t < 1.0) {
         t += Time.deltaTime;
         GetComponent.<AudioSource>().volume = t;
         yield;
	}
}
//----------------------------------------------------------fadeIn------------------------------------------------------------------------------

